# Part 0, Exercise 0.6

```mermaid
    sequenceDiagram
        participant browser
        participant server

        note right of browser: The note is added to the notes array locally.
        note right of browser: Notes are redrawn.

        browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
        activate server
        server-->>browser: HTTP 201 Created
        deactivate server
```